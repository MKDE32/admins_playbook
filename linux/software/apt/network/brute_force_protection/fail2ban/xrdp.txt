common xrdp log files:
/var/log/auth.log
/var/log/xrdp.log
/var/log/xrdp-sesman.log

>>check which log contains auth fails with ip adress
>> here it was xrdp-sesman.log
>> if no auth fail logging, set sudo nano /etc/xrdp/xrdp.ini to log_level=DEBUG and restart xrdp

sudo nano /etc/fail2ban/jail.local

[xrdp]
enabled  = true
port     = 3389
protocol = tcp
filter   = xrdp
logpath  = /var/log/xrdp-sesman.log
maxretry = 5
findtime = 600
bantime  = 3600
backend = auto

sudo nano /etc/fail2ban/filter.d/xrdp.conf

[Definition]
failregex = ^.*AUTHFAIL: user=.* ip=<HOST>.*$
ignoreregex =

sudo systemctl restart fail2ban
sudo fail2ban-client status

do a failed login attempt and after that test regex:
sudo fail2ban-regex /var/log/xrdp.log /etc/fail2ban/filter.d/xrdp.conf
total should be more than 0 else your failregex doesnt work
