get-disk    				auswahl datenträger
clear-disk    				löscht daten
initialize-disk    			initialisiert datenträger
set-disk    					ändert datenträger
volume						zeigt partitionen an
get-volume    			auswahl partition
format-volume  	  	formatiert partition
